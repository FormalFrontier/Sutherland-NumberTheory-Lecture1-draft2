## Accomplished

Stage 3.3 proof-filling: two more items proved.

- **`sutherland_corollary1_5_finite`** (Corollary1_5.lean): proved using `FiniteField.pow_card_sub_one_eq_one` + `one_lt_pow₀` / `pow_lt_one₀` to pin `f x ∈ {0,1}`. Added `import Mathlib.FieldTheory.Finite.Basic`.
- **`zsqrtd5_not_integrallyClosed`** (Example1_24.lean): proved by:
  1. Instantiating `Zsqrtd.Nonsquare 5` and `IsDomain (Zsqrtd 5)` by hand
  2. Embedding ℤ[√5] → ℝ via `Zsqrtd.toReal` (injective since 5 is not a perfect square)
  3. Lifting to `FractionRing (Zsqrtd 5) →+* ℝ` via `IsFractionRing.lift`
  4. Witness `x = ⟨1,1⟩/⟨2,0⟩` shown integral via `decide` on `a5² - a5·b5 - b5² = 0`
  5. Contradiction: if IC then `y.re + y.im·√5 = (1+√5)/2` implies √5 rational, but `Nat.Prime.irrational_sqrt` gives `Irrational (√5)`
  - Added imports: `Mathlib.Tactic.NormNum.Prime`, `Mathlib.NumberTheory.Zsqrtd.ToReal`, `Mathlib.RingTheory.Localization.FractionRing`
- Updated `progress/items.json`: Example1_24 → `proved`, Corollary1_5 → `proof_partial`

## Current frontier

`sutherland_corollary1_5_posChar` in Corollary1_5.lean still has a `sorry` (the proof that positive characteristic implies nonarchimedean). This is the harder case — needs `CharP.cast_eq_zero` or similar to show `f(n) ≤ 1` for all natural number inclusions.

## Overall project progress

- Stages 1.1–3.2: complete
- Stage 3.3 (proof-filling): 8/28 items fully proved (added Example1_24; Corollary1_5 partial)
  - Proved: Theorem1_8, Theorem1_9, Corollary1_21, Proposition1_18, Proposition1_22, Remark1_27, Example1_24, + partial Corollary1_5
  - 12/28 still have proof sorries; 10/28 are pure definitions (no proof needed)

## Next step

1. `sutherland_corollary1_5_posChar` — positive char → nonarchimedean. Key: `f(p·1) ≤ 1` since `(p·1)^p = p·1` (Frobenius), so `f(n)^p = f(n)`, meaning `f(n) ∈ {0,1}`. Look for `CharP.cast_eq_zero` and `ZMod` in Mathlib.
2. `Theorem1_16` — DVR ↔ local PID; assemble from Mathlib instances.

## Blockers

None.
