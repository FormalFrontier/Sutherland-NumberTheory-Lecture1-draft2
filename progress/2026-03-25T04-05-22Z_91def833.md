## Accomplished

Filled the last remaining sorry in the project: `zsqrtd5_not_integrallyClosed` in `Chapter1/Example1_24.lean`.

The proof shows `¬IsIntegrallyClosed (Zsqrtd 5)` by:
1. Establishing `Zsqrtd.Nonsquare (5 : ℕ)` to enable `IsDomain`/`Field` instances
2. Using `change` to convert `Zsqrtd 5` to `Zsqrtd ↑(5 : ℕ)` for instance resolution
3. Exhibiting `φ = (1+√5)/2` in `FractionRing (Zsqrtd 5)` as a witness:
   - Proving φ is integral: it satisfies `x² - x - 1 = 0` (key identity: `⟨1,1⟩² - ⟨1,1⟩·⟨2,0⟩ - ⟨2,0⟩² = 0` in ℤ[√5])
   - Proving φ ∉ image of algebraMap: if `2y = ⟨1,1⟩` in ℤ[√5], then `2·y.im = 1`, impossible in ℤ

Updated `progress/items.json`: Example1_24 promoted to `sorry_free`.

All 28 formalizable Chapter 1 items are now `sorry_free`. The project has **zero sorries**.

## Current frontier

All proof sorries filled. Stage 3.3 complete. Ready for Stage 3.5 (proof polishing) which is already claimed (issue #43).

## Overall project progress

- Stages 1.1–3.4: complete
- Stage 3.3: **complete** — all 28/28 items are now `sorry_free`
- Stage 3.5 (polish): claimed but not yet done
- Stage 3.6 (upstreaming analysis): not yet started

## Next step

Stage 3.5 (issue #43) is claimed. If that agent is stuck or done, proceed to Stage 3.6: upstreaming analysis. Candidates for upstreaming include `zsqrtd5_not_integrallyClosed` (novel result about ℤ[√5]).

## Blockers

None.
