## Accomplished

Filled the `sorry` in `zsqrtd5_not_integrallyClosed` in `SutherlandNumberTheoryLecture1Draft2/Chapter1/Example1_24.lean`, completing issue #32 (Stage 3.3e).

The proof demonstrates that ℤ[√5] is not integrally closed by:
1. Proving `φ = ⟨1,1⟩/2 ∈ FractionRing(Zsqrtd 5)` satisfies `X²-X-1=0` (integral over ℤ[√5])
2. Deriving a contradiction: if `φ ∈ ℤ[√5]`, then `2·a = ⟨1,1⟩` for some `a : Zsqrtd 5`, but `2·a.re = 1` has no integer solution

Key technical fixes during development:
- Used `IsDomain (ℤ√(5 : ℕ))` to get `NoZeroDivisors` (needed by `mem_nonZeroDivisors_of_ne_zero`)
- Polynomial evaluation: replaced broken `have hring := by ring` (which can't handle `⁻¹`) with `field_simp [h2'] + linear_combination hkey`
- Used `simp only [eval₂_sub, eval₂_pow, eval₂_X, eval₂_one, φ]` to expand `aeval`
- Used `decide` to evaluate `(2 : Zsqrtd 5).re = 2` and `.im = 0` for the omega contradiction

`Example1_29.lean` was already fully proved (confirmed at session start).

PR: https://github.com/FormalFrontier/Sutherland-NumberTheory-Lecture1-draft2/pull/56

## Current frontier

Issue #32 complete. `Example1_24` and `Example1_29` are both fully proved.

## Overall project progress

- Stage 3.2 complete (all 28 items have Lean scaffolding, definition_verified)
- Stage 3.3 partially complete: 6+ items fully proved (no sorries)
  - Proposition1_20, Proposition1_25, Corollary1_23 (Stage 3.3 batch from PR #37)
  - Example1_24 (this session), Example1_29 (pre-existing)
  - Theorem1_16 (pre-existing, may need verification)

## Next step

Work on remaining Stage 3.3 items. Priority:
- `Theorem1_16` (UFD implies PID for PIDs — need to verify proof completeness)
- `Definition1_1` through `Definition1_18` (foundational definitions that others depend on)
- Check `progress/items.json` for items with `status: "definition_verified"` that still have sorries

## Blockers

None.
