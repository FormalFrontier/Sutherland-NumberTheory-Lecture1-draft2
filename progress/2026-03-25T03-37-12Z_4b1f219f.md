## Accomplished

Stage 3.3d (issue #34): Fill proof sorries in 01_04_Lemma and 01_05_Corollary.

- **01_04_Lemma (reverse direction)**: If `f(n) ≤ 1` for all `n : ℕ`, then `f` is nonarchimedean.
  - Strategy: use `AbsoluteValue.toNormedField` to get a `NormedField k` with norm = f, apply
    `IsUltrametricDist.isUltrametricDist_of_forall_norm_natCast_le_one` (Mathlib's binomial
    theorem argument), then `IsUltrametricDist.isNonarchimedean_norm`.
  - New imports: `Mathlib.Analysis.Normed.Field.Basic`, `.Field.Ultra`, `.Group.Ultra`

- **01_05_Corollary_posChar**: Every absolute value on a pos-char field is nonarchimedean.
  - Strategy: apply 01_04_Lemma; show `f(n)^p = f(n)` for all n by induction using `add_pow_char`;
    deduce `f(n) ≤ 1` from `f(n)^p = f(n)` using `pow_lt_pow_right₀` contradiction.
  - Key steps: `CharP.char_prime_of_ne_zero`, `haveI : Fact p.Prime`, induction with `add_pow_char`.

- **01_05_Corollary_finite**: The only absolute value on a finite field is trivial.
  - Strategy: show `f(x) = 1` for all `x ≠ 0` via `FiniteField.pow_card_sub_one_eq_one` and
    `pow_eq_one_iff_of_nonneg`.
  - New import: `Mathlib.FieldTheory.Finite.Basic`

- Updated `progress/items.json`: 01_04_Lemma and 01_05_Corollary promoted to `sorry_free`.

Also skipped issue #30 (01_28_Proposition already sorry_free from commit 13445a2).

## Current frontier

- Remaining sorries: only `01_24_Example/zsqrtd5_not_integrallyClosed` (issue #32 or #35)
- 01_04_Lemma, 01_05_Corollary are now fully proved

## Overall project progress

- Stages 1.1–3.2: complete
- Stage 3.3 proof-filling: 20/28 items `sorry_free`, 7/28 `definition_verified` (all defs), 1/28 partial
  - Newly sorry_free: 01_04_Lemma, 01_05_Corollary
  - Remaining sorry: only 01_24_Example (zsqrtd5_not_integrallyClosed)

## Next step

1. Fix 01_24_Example (`zsqrtd5_not_integrallyClosed`) — needs to show `ℤ[√5]` is not integrally closed
2. Merge any outstanding PRs

## Blockers

None.
