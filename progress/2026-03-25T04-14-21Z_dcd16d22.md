## Accomplished

Stage 3.5 polish and upstreaming addendum for `Example1_24` (issue #51).

**Polish:**
- Simplified `hkey` proof in `zsqrtd5_not_integrallyClosed`: replaced 4-line
  `congr_arg` + `rw` + `simp` + `exact` pattern with a single `simpa` call.
- Changed two `from by decide` proofs in `hre` simp set to `from rfl` (computationally
  definitional, no tactic needed).
- Build passes: `lake build SutherlandNumberTheoryLecture1.Chapter1.Example1_24` ✓

**Items update:**
- `progress/items.json`: `Chapter1/Example1_24` advanced from `definition_verified`
  to `proof_polished`; `upstreaming_status: "candidate"` added.

**Upstreaming addendum:**
- `UPSTREAMING.md` summary updated: 4 candidates (was 3).
- Full candidate entry added for `Example1_24 — ℤ[√5] is Not Integrally Closed`:
  verdict `candidate`. Mathlib search confirmed no `IntegrallyClosed`/
  `isIntegrallyClosed` results in any `Zsqrtd` file.
- Search notes updated with Zsqrtd search result.

## Current frontier

All Chapter 1 items are now at `proof_polished` or beyond (or `extracted` for
discussion blobs). The three upstreaming candidates (Lemma1_4, Corollary1_5,
Proposition1_28, Example1_24) are documented in `UPSTREAMING.md`.

## Overall project progress

- Stages 1–3.6 complete.
- Stage 3.5 addendum (Example1_24 polish): complete.
- All 28 formalizable Chapter 1 items: `proof_polished` or equivalent.
- 4 upstreaming candidates identified and documented.

## Next step

The project is effectively complete for Chapter 1. Next steps (if any):
- Open Mathlib PRs for the 4 upstreaming candidates.
- Move to Chapter 2 (requires a new PLAN.md / planner session).

## Blockers

None.
