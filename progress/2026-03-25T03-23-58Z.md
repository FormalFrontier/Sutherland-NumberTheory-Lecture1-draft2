## Accomplished

Stage 3.3 proof-filling session (issue #26). Eliminated sorries across 10 items:

- **01.20.Proposition** (integrality is transitive): proved using `Algebra.IsIntegral.trans B` via `haveI` to introduce typeclass instances from explicit hypotheses.
- **01.25.Proposition** (valuation rings are integrally closed): `ValuationRing → IsBezout → Nonempty(GCDMonoid) → GCDMonoid.toIsIntegrallyClosed`.
- **01.23.Corollary** (UFDs are integrally closed): `UniqueFactorizationMonoid.toGCDMonoid` then `GCDMonoid.toIsIntegrallyClosed`.
- **01.14.Example** (prime ideal in ℤ): `Ideal.span_singleton_prime` + `Nat.prime_iff_prime_int`.
- **01.15.Example** (power series DVR): `inferInstance` (the `IsDiscreteValuationRing (PowerSeries k)` instance is provided by `Mathlib.RingTheory.PowerSeries.Inverse`).
- **01.03.Example** (`padicAbsoluteValue_isNonarchimedean`): `padicNorm.nonarchimedean` via `Rat.AbsoluteValue.padic_eq_padicNorm`.
- **01.16.Theorem** (DVR ↔ local PID): both directions — definition unpacking + `IsDiscreteValuationRing.not_isField`.
- **01.28.Proposition** (minpoly characterization for integrally closed domains): `minpoly.isIntegrallyClosed_eq_field_fractions'` for one direction; `minpoly.monic.ne_zero` + `Algebra.IsIntegral.of_finite` for the other.
- **01.24.Example** (`goldenRatio_isIntegral`): `compute_degree!` for monicness + `nlinarith [Real.sq_sqrt]` for eval.
- **01.29.Example** (`halfOnePlusSqrt7_not_integral`): product argument — `(1+√7)/2 · (1-√7)/2 = -3/2` by `Real.sq_sqrt`; lift via `isIntegral_algebraMap_iff`; contradict `IsIntegrallyClosed.isIntegral_iff` for ℤ via `omega`.

Updated `progress/items.json`: 25 items promoted to `sorry_free`.

## Current frontier

**4 remaining sorries** across 3 files:
- `01.04.Lemma.lean` (1 sorry): backward direction of the non-archimedean equivalence. Requires a binomial theorem + limit/completeness argument about `|x^n|^{1/n}`. Hard to formalize directly.
- `01.05.Corollary.lean` (2 sorries): depends on 01.04.Lemma.
- `01.24.Example.lean` (1 sorry): `zsqrtd5_not_integrallyClosed`. Requires constructing the fraction ring of `Zsqrtd 5` and showing `(1+√5)/2 ∉ ℤ[√5]` while being integral.

## Overall project progress

Stage 3.3 is well underway:
- **25/28 formalizable items** are now `sorry_free`
- **3 items remain** at `definition_verified` with hard-to-fill sorries
- Previous stages (1.x, 2.x, 3.1, 3.2) are all complete

## Next step

Option A: Attempt `zsqrtd5_not_integrallyClosed` in 01.24.Example — the key is to show `(1+√5)/2` is in `FractionRing (Zsqrtd 5)` but not in `Zsqrtd 5`. Approach: use `Zsqrtd.nonneg_cases` or direct Zsqrtd membership check.

Option B: Attempt 01.04.Lemma backward direction. The book's proof uses the fact that if `|x^n|^{1/n} → 1` for all `x`, then the absolute value is non-archimedean. This requires a limit/completeness argument. May need `Mathlib.Topology.Algebra.Order.LiminfLimsup`.

Option C: Proceed to Stage 3.4 (dependency trimming) for the 25 `sorry_free` items — removing transitive dependencies from `dependencies/internal.json`.

## Blockers

- `01.04.Lemma` backward direction: binomial theorem + limit argument; hard in Lean without dedicated infrastructure
- `01.24.Example.zsqrtd5_not_integrallyClosed`: fraction ring of `Zsqrtd 5` is nonstandard; `IsIntegrallyClosed (Zsqrtd 5)` requires showing the ring fails the condition
