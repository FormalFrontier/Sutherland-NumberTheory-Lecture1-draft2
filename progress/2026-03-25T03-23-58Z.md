## Accomplished

Stage 3.3 proof-filling session (issue #26). Eliminated sorries across 10 items:

- **Proposition1_20** (integrality is transitive): proved using `Algebra.IsIntegral.trans B` via `haveI` to introduce typeclass instances from explicit hypotheses.
- **Proposition1_25** (valuation rings are integrally closed): `ValuationRing → IsBezout → Nonempty(GCDMonoid) → GCDMonoid.toIsIntegrallyClosed`.
- **Corollary1_23** (UFDs are integrally closed): `UniqueFactorizationMonoid.toGCDMonoid` then `GCDMonoid.toIsIntegrallyClosed`.
- **Example1_14** (prime ideal in ℤ): `Ideal.span_singleton_prime` + `Nat.prime_iff_prime_int`.
- **Example1_15** (power series DVR): `inferInstance` (the `IsDiscreteValuationRing (PowerSeries k)` instance is provided by `Mathlib.RingTheory.PowerSeries.Inverse`).
- **Example1_3** (`padicAbsoluteValue_isNonarchimedean`): `padicNorm.nonarchimedean` via `Rat.AbsoluteValue.padic_eq_padicNorm`.
- **Theorem1_16** (DVR ↔ local PID): both directions — definition unpacking + `IsDiscreteValuationRing.not_isField`.
- **Proposition1_28** (minpoly characterization for integrally closed domains): `minpoly.isIntegrallyClosed_eq_field_fractions'` for one direction; `minpoly.monic.ne_zero` + `Algebra.IsIntegral.of_finite` for the other.
- **Example1_24** (`goldenRatio_isIntegral`): `compute_degree!` for monicness + `nlinarith [Real.sq_sqrt]` for eval.
- **Example1_29** (`halfOnePlusSqrt7_not_integral`): product argument — `(1+√7)/2 · (1-√7)/2 = -3/2` by `Real.sq_sqrt`; lift via `isIntegral_algebraMap_iff`; contradict `IsIntegrallyClosed.isIntegral_iff` for ℤ via `omega`.

Updated `progress/items.json`: 25 items promoted to `sorry_free`.

## Current frontier

**4 remaining sorries** across 3 files:
- `Lemma1_4.lean` (1 sorry): backward direction of the non-archimedean equivalence. Requires a binomial theorem + limit/completeness argument about `|x^n|^{1/n}`. Hard to formalize directly.
- `Corollary1_5.lean` (2 sorries): depends on Lemma1_4.
- `Example1_24.lean` (1 sorry): `zsqrtd5_not_integrallyClosed`. Requires constructing the fraction ring of `Zsqrtd 5` and showing `(1+√5)/2 ∉ ℤ[√5]` while being integral.

## Overall project progress

Stage 3.3 is well underway:
- **25/28 formalizable items** are now `sorry_free`
- **3 items remain** at `definition_verified` with hard-to-fill sorries
- Previous stages (1.x, 2.x, 3.1, 3.2) are all complete

## Next step

Option A: Attempt `zsqrtd5_not_integrallyClosed` in Example1_24 — the key is to show `(1+√5)/2` is in `FractionRing (Zsqrtd 5)` but not in `Zsqrtd 5`. Approach: use `Zsqrtd.nonneg_cases` or direct Zsqrtd membership check.

Option B: Attempt Lemma1_4 backward direction. The book's proof uses the fact that if `|x^n|^{1/n} → 1` for all `x`, then the absolute value is non-archimedean. This requires a limit/completeness argument. May need `Mathlib.Topology.Algebra.Order.LiminfLimsup`.

Option C: Proceed to Stage 3.4 (dependency trimming) for the 25 `sorry_free` items — removing transitive dependencies from `dependencies/internal.json`.

## Blockers

- `Lemma1_4` backward direction: binomial theorem + limit argument; hard in Lean without dedicated infrastructure
- `Example1_24.zsqrtd5_not_integrallyClosed`: fraction ring of `Zsqrtd 5` is nonstandard; `IsIntegrallyClosed (Zsqrtd 5)` requires showing the ring fails the condition
